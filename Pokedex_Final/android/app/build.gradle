plugins {
    id "com.android.application"
    id "kotlin-android"
    id "dev.flutter.flutter-gradle-plugin" // Flutter plugin debe ir después de Android y Kotlin
}

android {
    namespace = "com.example.pokedex_final"
    compileSdk = 34

    compileOptions {
        sourceCompatibility = JavaVersion.VERSION_11
        targetCompatibility = JavaVersion.VERSION_11
    }

    kotlinOptions {
        jvmTarget = "11" // Asegúrate de que esté entre comillas
    }

    defaultConfig {
        applicationId = "com.example.pokedex_final"
        minSdk = 21
        targetSdk = 34
        versionCode = 1
        versionName = "1.0"
    }

    buildTypes {
        release {
            // Configuración para evitar la firma en el modo release si no tienes configurada una clave de firma
            minifyEnabled false
            shrinkResources false
            // Configuración de firma de release
            signingConfig = signingConfigs.debug
        }
    }

}

flutter {
    source = "../.."
}

dependencies {
    // Aquí puedes agregar otras dependencias de Android si es necesario
    implementation "androidx.core:core-ktx:1.7.0"
    implementation "androidx.appcompat:appcompat:1.4.0"
    implementation "com.google.android.material:material:1.4.0"
}
